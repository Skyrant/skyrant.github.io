rh._.exports({"0":[["Tasks und Abhängigkeiten"]],"1":[["\n"],["\n","Die verfügbaren Tasks sind in der Tabelle se_tasks \n gespeichert. Die Abhängigkeiten einzelner Task-Typen voneinander ist in \n der Spalte \"depends_on\" der Tabelle se_task_types hinterlegt.","\n","Pro Task-Typ und SE-Instanz kann je eine Task angelegt \n werden. Wird dabei kein Eintrag in der Spalte \"Properties\" vorgenommen, \n werden die Eigenschaften des Task-Typs verwendet. Dabei ist es auch möglich, \n nur einzelne Zeilen der Eigenschaften für bestimmte Tasks anders zu definieren \n als im Standard.","\n","Wichtig ist auch das Feld \"Active\": ","\n","Wert \n 1 bedeutet, dass die Task geladen werden soll.","\n","Wert \n 0 bedeutet, dass die Task nur geladen wird, falls eine andere Task sie \n benötigt.","\n"," ","\n","Damit kann man also Konfigurationen auch für Tasks \n angeben, die nicht explizit geladen werden sollen.","\n","Beispiel: Die Task \"functions\" wird normalerweise \n nicht explizit zum Laden angegeben,  aber in der Regel trotzdem \n benötigt (sie findet z.B. die auszuführenden Skript-Funktionen).  Damit \n kann man dann trotzdem eine Konfiguration angeben (z.B. \"updateCheckSeconds\" \n für das Intervall, in dem sie nach geänderten Skripten suchen soll).","\n","Das gleiche gilt auch für die Task \"webserver\", \n die i.d.R. auch nicht explizit angegeben wird, sondern für \"webapp\", \n \"jsonapi\" oder \"jsonrpc\" geladen wird. Dennoch werden \n natürlich Konfigurations-Einträge für die Task benötigt.","\n","Es ist also zu beachten, dass \"active = 0\" \n eben nicht wie bei SE-Jobs bedeutet, das die Task von der betreffenden \n Instanz gar nicht ausgeführt wird. Sie ist also weder \"deaktiviert\", \n noch \"ausgeschaltet\" oder gar \"verboten\".","\n\n\n"]],"2":[["Verfügbare Tasks und deren Abhängigkeiten"]],"5":[["Task"]],"id":"20"})